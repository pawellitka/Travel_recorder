import { Routes } from '@angular/router';
import { TravelTracksComponent } from './travel-tracks/travel-tracks.component';

export const routes: Routes = [
  { path: '', component: TravelTracksComponent },
];
